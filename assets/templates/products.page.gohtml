{{template "base" .}}

{{define "content" }}
    <div class="contentBody">
        <h1>{{.Name}}</h1>
        <div class="gridContainer">
            {{range .Products}}
                {{template "grid_product_item_in_list" .}}
            {{end}}
        </div>
    </div>
{{end}}